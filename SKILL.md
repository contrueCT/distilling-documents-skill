---
name: distilling-documents
description: Use when a user has a long Chinese document, article, report, technical note, research material, or information source that is costly to reread and wants a compact replacement that remains useful for later lookup.
---

# Distilling Documents

## Overview

把长材料蒸馏成一份能长期替代原文的中文精炼稿。用户没有说明阅读目的时，默认假设原文以后不会再读；输出要同时承担理解、回忆和查询作用。

**核心目标：最小化「读精炼稿的成本 + 未来回看原文的成本」；压缩率只是手段。**

默认零交互：不要追问阅读目的、目标长度或保留比例。普通任务只交付最终精炼稿，不展示信息账本、评分、删除清单或检查过程。

**中文表达层：** 使用包内 `references/human-writing-adapted.md` 的规则做最终改写；该适配层可独立运行，不要求外部安装 `human-writing`。

## Workflow

### Pass 1：高保真蒸馏

1. 自动判断材料类型和原本承担的功能。
2. 内部识别关键事实、数字、时间/版本、定义、机制、因果、条件、边界、不确定性、证据、反例和重要判断。
3. 打破原文顺序，按语义关系聚类。提高密度时优先 `MERGE → COMPRESS → DROP`。
4. 生成可独立理解的 information packet：主信息 + 必要数据/依据 + 关系/含义 + 必要边界。不要机械凑齐四项。
5. 对关键包保留内部来源锚点（页码、章节或段落）。默认不展示。

### Pass 2：改写 + 对照校验

1. 根据材料类型选择最终结构和语体；按 `references/human-writing-adapted.md` 改成自然中文，但禁止表达层重新决定信息删留。
2. 对照原文复核：关键数字/条件是否漏掉；推测是否变成事实；结论是否丢失必要依据；反例/限制是否消失；有没有新增原文不存在的事实。
3. 发现问题直接修复，不让用户参与审核。

普通材料到此结束。超长或高密度材料可以自动分块，但分块结果只作为临时状态，不落盘、不交付；最后仍只输出一份全局精炼稿。

## Distillation Rules

默认生成“通用替代稿”。用户明确给出目标、篇幅或用途时，以用户目标覆盖默认值。

- **保留可重新推理的信息。** 关键事实、数字、条件和关系通常比“门槛高”“能力强”这类二次概括更有价值。
- **合并优先于删除。** 三段可以压成一句时，优先重组，不要只留结论。
- **未来查询价值也算价值。** 版本、价格、阈值、配置、名称、时间、License 条件等，即使不是主旨，也常值得保留。
- **认识状态必须保真。** “官方确认 / 原文声称 / 估算 / 作者判断 / 推测 / 预测”不能互相升级。
- **精确信息不得模糊化。** 关键数字、专有名词、版本、阈值和否定条件需要 literal-preserve。
- **真正 DROP 要保守。** 拿不准时多留一点，不要把选择重新丢给用户。

长材料或删留不确定时，读取 `references/distillation-rules.md`。

## Output Routing

最终形态由内容功能决定，不套统一“摘要模板”。技术/设计文档保留结构和术语；分享文章保留来龙去脉；信息资料强调高密度和可查询性；教程保留步骤和必要解释。

路由不明确，或担心自然化改写破坏技术结构时，读取 `references/routing-and-rewrite.md`。

## Output Contract

- 默认只输出最终精炼稿。
- 长度由信息密度决定，不追求固定压缩比例。
- 标题、段落、列表、表格按内容需要使用，不为“简洁”强行统一成 bullet list。
- 默认隐藏来源锚点；用户要求可追溯，或某个关键争议/精确引语确实需要核查时再展示。
- 如果源材料本身缺失、无法读取或存在无法消除的关键缺口，明确告诉用户哪些部分尚不能被替代。

## Quick Reference

| 情况 | 默认动作 |
|---|---|
| 多处重复同一事实 | MERGE |
| 解释很长但核心机制可保留 | COMPRESS |
| 只有铺垫/过渡且无独立价值 | DROP |
| 数字、阈值、版本、License 条件 | KEEP / MERGE，保留精度 |
| 结论依赖某个原因或证据 | 一起放入 information packet |
| 作者推测或估算 | 保留认识状态 |
| 正反证据同时存在 | 两边都保留，避免改写整体判断 |

## Common Mistakes

- 把“精炼”写成主旨摘要，只剩抽象结论。
- 保留所有 facts，却让读者自己拼因果关系。
- 为追求短，把具体数字改成“较大”“较高”“一定规模”。
- 自然化改写时顺手删掉条件、术语或不确定性。
- 忠于原文顺序，导致同一主题散落多处。
- 默认生成信息账本、评分表、Probe 问答等中间产物。
- 为了让流程“更准确”频繁追问用户，抵消省事的目标。
